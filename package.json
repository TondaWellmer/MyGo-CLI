{
  "name": "mygo-cli",
  "version": "0.1.0",
  "description": "AI Chat Terminal — Free & Premium Models mit Multi-Login",
  "main": "src/main/main.js",
  "scripts": {
    "compile": "esbuild src/renderer/terminal.js --bundle --outfile=dist/terminal.bundle.js --platform=node --external:electron && esbuild src/renderer/sidebar.js --bundle --outfile=dist/sidebar.bundle.js --platform=browser --external:electron && esbuild src/renderer/titlebar.js --bundle --outfile=dist/titlebar.bundle.js --platform=browser --external:electron",
    "build": "npm run compile",
    "start": "npm run compile && electron .",
    "dev": "npm run compile && electron . --dev",
    "package": "npm run compile && electron-builder --win portable"
  },
  "build": {
    "appId": "com.mygo.cli",
    "productName": "MyGo-CLI",
    "npmRebuild": false,
    "directories": {
      "output": "release"
    },
    "files": [
      "src/**/*",
      "dist/**/*",
      "assets/**/*",
      "node_modules/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ]
    },
    "portable": {
      "artifactName": "MyGo-CLI-${version}-portable.exe"
    }
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "esbuild": "^0.27.4",
    "ws": "^8.20.0"
  }
}
